spring.application.name=hnh

# Database Configuration
# spring.datasource.url=jdbc:mysql://localhost:3306/heng_nguonhour_db?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=Hnh00900
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Test Database Override Engine settings
spring.datasource.url=jdbc:sqlite:file:testdb?mode=memory&cache=shared
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=create-drop

# Server Configuration
server.port=8090

# JPA / Hibernate properties
# spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Flyway Configuration
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration